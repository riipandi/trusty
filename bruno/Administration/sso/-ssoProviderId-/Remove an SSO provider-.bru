meta {
  name: Remove an SSO provider-
  type: http
  seq: 3
}

delete {
  url: {{BASE_URL}}/admin/sso/providers/:ssoProviderId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}
