meta {
  name: Delete a user
  type: http
  seq: 4
}

delete {
  url: {{BASE_URL}}/admin/users/:userId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

docs {
  Delete a user
}
